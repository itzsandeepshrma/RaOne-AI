import pickle
import os

MODEL_PATH = os.path.join(os.path.dirname(__file__), "adaptive_learning_model.pkl")

def load_adaptive_model():
    with open(MODEL_PATH, 'rb') as file:
        model = pickle.load(file)
    return model
