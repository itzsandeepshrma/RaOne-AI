import pickle
import os

MODEL_PATH = os.path.join(os.path.dirname(__file__), "multimodal_interaction_model.pkl")

def load_multimodal_model():
    with open(MODEL_PATH, 'rb') as file:
        model = pickle.load(file)
    return model
