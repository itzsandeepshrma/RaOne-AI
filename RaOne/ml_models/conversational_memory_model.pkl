import pickle
import os

MODEL_PATH = os.path.join(os.path.dirname(__file__), "conversational_memory_model.pkl")

def load_memory_model():
    with open(MODEL_PATH, 'rb') as file:
        model = pickle.load(file)
    return model
